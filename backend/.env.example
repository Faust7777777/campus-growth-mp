# 复制本文件为 .env 并填入真实值。.env 不要提交到仓库，也不要出现在小程序前端。
# DeepSeek 为 OpenAI 兼容接口。

OPENAI_API_KEY=sk-你的DeepSeek密钥
OPENAI_BASE_URL=https://api.deepseek.com/v1
OPENAI_MODEL=deepseek-chat

# 服务端口（小程序前端请求 http://127.0.0.1:3000）
PORT=3000

# 单次 AI 调用超时（毫秒），超时自动走 fallback 演示样例
AI_TIMEOUT_MS=20000
